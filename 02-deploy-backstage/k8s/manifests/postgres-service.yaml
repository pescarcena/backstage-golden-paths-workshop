apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: backstage
  labels:
    app: postgres
    app.kubernetes.io/part-of: backstage
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
